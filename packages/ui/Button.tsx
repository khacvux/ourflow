import * as React from "react";
import { Button as ChakraButton } from "@chakra-ui/react";

export const Button = () => {
  return (
    <ChakraButton className="rounded-full  text-white !bg-blue-900">
      Boop
    </ChakraButton>
  );
};
